title: Add functions to control statement store per-account allowances
doc:
- audience: Node Dev
  description: 'The title says it. The functions currently residing in `individuality`
    must be moved to Substrate to unify storage allowance control. '
crates:
- name: sp-statement-store
  bump: minor
